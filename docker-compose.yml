services:
  spa-server:
    image: httpd:2.4-alpine
    container_name: react_apache_spa
    ports:
      - "8080:80"
    volumes:
      - ./dist:/usr/local/apache2/htdocs/:ro
      - ./container/vhost.conf:/usr/local/apache2/conf/httpd.conf:ro